server:
  port: 8083

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ValentinoL15/Tienda_Microservicios.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}